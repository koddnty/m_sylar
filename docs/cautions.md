# 本为将对框架使用的常见问题进行说明

## 框架基本使用

在框架中，由ioManager创建的线程池中，已经实现的hook函数默认开启。在主线程中，仍使用系统默认调用。

每个ioManager对应一个线程池，初始情况下，如果运行代码中没有使用到timer定时器相关功能，将不会对其进行初始化。

timerManager类与ioManager类是多对一依赖关系, 多个timemanager使用一个相同的iomanager进行定时器任务, 应注意为定时器任务与其对应定时器。应避免取消注册在其他定时器Manager中的定时器。
